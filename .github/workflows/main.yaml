name: exec rebalancing

on:
  workflow_dispatch:

jobs:
  build:
    name: rebalance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install -r requirments.txt
      - run: export PYTHONPATH=.
      - run: python src/main.py